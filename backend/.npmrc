# Skip Puppeteer Chromium download during installation
# This prevents build timeouts on Heroku/Render
# 
# IMPORTANT: Puppeteer checks process.env.PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
# This .npmrc file helps with npm/yarn config, but the environment variable
# must be set in the deployment platform (Heroku/Render) config vars.
# 
# For Heroku: heroku config:set PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
# For Render: Set in Environment Variables section
puppeteer_skip_chromium_download=true

